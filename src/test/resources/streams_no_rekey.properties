application.id=joining-streams
bootstrap.servers=localhost:9092
schema.registry.url=http://localhost:8081
auto.offset.reset=earliest

replication.factor=3
num.partitions=1

input_one.topic=topic_left_side_input
input_two.topic=topic_right_side_input
join.output.topic=topic_join_output